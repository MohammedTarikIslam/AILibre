{"file": "sal/osl/unx/random.cxx", "chunk_id": 0, "code": "// FILE: sal/osl/unx/random.cxx\n// CHUNK 0 | estimated_tokens: 353\n\n#include <assert.h>\n#include <errno.h>\n#include <fcntl.h>\n#include <unistd.h>\n#include <dlfcn.h>\nbool osl_get_system_random_data(char* buffer, size_t desired_len)\n{\n    int fd;\n\n    assert(buffer);\n\n    static int (*lok_open_urandom)()\n        = reinterpret_cast<int (*)()>(dlsym(RTLD_DEFAULT, \"lok_open_urandom\"));\n    if (!lok_open_urandom || (fd = lok_open_urandom()) < 0)\n        fd = open(\"/dev/urandom\", O_RDONLY);\n\n    if (fd != -1)\n    {\n        while (desired_len)\n        {\n            ssize_t nb_read;\n            if ((nb_read = read(fd, buffer, desired_len)) < 0)\n            {\n                if (errno != EINTR)\n                {\n                    close(fd);\n                    return false;\n                }\n            }\n            else\n            {\n                buffer += nb_read;\n                desired_len -= nb_read;\n            }\n        }\n        close(fd);\n        return true;\n    }\n    return false;\n}", "tokens": 388}
