{"file":"gpuai_env/lib/python3.10/site-packages/include/ggml-opt.h","chunk_id":0,"code":"// FILE: gpuai_env/lib/python3.10/site-packages/include/ggml-opt.h\n// CHUNK 0 | estimated_tokens: 686\n\n#endif\nruct ggml_opt_dataset;\n#endif\nruct ggml_opt_context;\n#endif\nruct ggml_opt_result;\n#endif\nruct ggml_opt_dataset *\n#endif\nruct ggml_opt_context *\n#endif\nruct ggml_opt_result\n// get batch at position ibatch from dataset and copy the data to data_batch and labels_batch\nruct ggml_tensor *\n// get batch at position ibatch from dataset and copy the data to data_batch and labels_batch\nruct ggml_tensor *\n// parameters that control which optimizer is used and how said optimizer tries to find the minimal loss\nruct ggml_opt_optimizer_params {\n        // AdamW optimizer parameters\n        struct {\n            float alpha; // learning rate\n            float beta1;\n            float beta2;\n            float eps;   // epsilon for numerical stability\n            float wd;    // weight decay for AdamW, use 0.0f to disable\n        } adamw;\n    };\n// callback to calculate optimizer parameters prior to a backward pass\n// userdata can be used to pass arbitrary data\nruct ggml_opt_optimizer_params (\n// parameters for initializing a new optimization context\nruct ggml_opt_params {\n        ggml_backend_sched_t backend_sched; // defines which backends are used to construct the compute graphs\n\n        struct ggml_context * ctx_compute; // created in user code, holds non-static tensors\n\n        // the forward graph is defined by inputs and outputs\n        // those tensors and all tensors inbetween are not intended to be reusable between multiple optimization contexts\n        struct ggml_tensor * inputs;\n        struct ggml_tensor * outputs;\n\n        enum ggml_opt_loss_type  loss_type;\n        enum ggml_opt_build_type build_type;\n\n        int32_t opt_period; // after how many gradient accumulation steps an optimizer step should be done\n\n        ggml_opt_get_optimizer_params get_opt_pars; // callback for calculating optimizer parameters\n        void * get_opt_pars_ud;                     // userdata for calculating optimizer parameters\n    };\n// get parameters for an optimization context with defaults set where possible\n// parameters for which no sensible defaults exist are supplied as arguments to this function\nruct ggml_context\n// get parameters for an optimization context with defaults set where possible\n// parameters for which no sensible defaults exist are supplied as arguments to this function\nruct ggml_tensor\n// get parameters for an optimization context with defaults set where possible\n// parameters for which no sensible defaults exist are supplied as arguments to this function\nruct ggml_tensor\n// get parameters for an optimization context with defaults set where possible\n// parameters for which no sensible defaults exist are supplied as arguments to this function\nruct ggml_opt_params p\n// get underlying tensors that store data\nruct ggml_tensor *","tokens":720}
